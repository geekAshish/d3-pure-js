
export const menu = () => {

  const my = (selection) => {
    selection.text('Ashish');
  };

  // 15:44
  // my.width = function (_) {
  //   return arguments.length ? ((width = +_), my) : width;
  // };

  return my;
};
